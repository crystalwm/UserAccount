import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'layout-basic-form',
    templateUrl: './layoutBasicForm.html',
})
export class LayoutBasciFormComponent {

    constructor() {
    }

    isChecked: boolean = false;
}
