import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'layout-inline-form',
    templateUrl: './layoutInlineForm.html',
})
export class LayoutInlineFormComponent {

    constructor() {
    }

    isRemember: boolean = false;
}
