import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'layout-horizontal-form',
    templateUrl: './layoutHorizontalForm.html',
})
export class LayoutHorizontalForm {

    constructor() {
    }

    isRemember: boolean = false;
}
