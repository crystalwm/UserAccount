import { Component } from '@angular/core';

@Component({
    moduleId: module.id,
    selector: 'tables',
    template: `
        <router-outlet></router-outlet>
        `
})
export class TablesComponent { }